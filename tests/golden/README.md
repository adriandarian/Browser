# Golden Artifacts

- `expected/`: committed golden baselines (`.rgba`, `.json`, `.sha256`).
- `actual/`: outputs from the latest test run (ignored except `.gitkeep`).
- `diff/`: per-fixture mismatch reports (ignored except `.gitkeep`).
